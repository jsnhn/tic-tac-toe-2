1) Define required constants

2) Define required variables used to track the state of the game

3) Store elements on the page that will be accessed in code more than once in variables to make code more concise, readable and performant.

4) Upon loading the app should:
  4.1) Initialize the state variables
  4.2) Render those values to the page
  4.3) Wait for the user to click a square

5) Handle a player clicking a square

6) Handle a player clicking the replay button

What to do to make tic tac toe work?
need to establish a 3x3 board to play on.
the board needs to be empty(null) until a players starts
we need to alternate turns
board needs to display that an event/action has been taken
when it is a cats game we need to show that it is a tie 
when a player wins, game needs to indicate who wins
there needs to be a score tally for winners/ties.



